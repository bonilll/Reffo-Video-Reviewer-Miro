"use client";

interface CalendarButtonProps {
  boardId?: string;
  size?: "sm" | "md" | "lg";
  onHover?: (label: string) => void;
  onHoverEnd?: () => void;
}

export const CalendarButton = (_props: CalendarButtonProps) => {
  return null;
};

export default CalendarButton;
